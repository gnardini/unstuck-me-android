<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/red_dark</item>
        <item name="colorAccent">@color/red</item>
    </style>

    <!-- Create Questions Styles -->

    <style name="create_questions_image_quarter_screen">
        <item name="android:layout_width">0px</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">0.5</item>
        <item name="android:layout_margin">@dimen/spacing_tiny</item>
        <item name="android:background">@color/dark_gray</item>
    </style>

    <style name="image_upload">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:scaleType">centerCrop</item>

        <item name="android:colorBackground">@color/gray</item>
    </style>

    <style name="create_questions_placeholder">
        <item name="android:src">@drawable/add_image</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="create_questions_button_remove_image">
        <item name="android:layout_width">@dimen/button_height_short</item>
        <item name="android:layout_height">@dimen/button_height_short</item>
        <item name="android:background">@drawable/ic_no_np</item>
    </style>

    <style name="create_questions_end">
        <item name="android:textSize">@dimen/font_larger</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_marginTop">@dimen/spacing_normal</item>
    </style>

    <!-- Crop image -->


    <style name="crop_image_style">
        <item name="android:textSize">@dimen/font_larger</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor"> @color/white</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!-- End Create Questions Styles -->

    <style name="QuestionsImageContainer" >
        <item name="android:layout_margin">@dimen/spacing_tiny</item>
    </style>

    <style name="QuestionsImage" >
        <item name="android:scaleType">centerCrop</item>
        <item name="android:background">@color/gray</item>
    </style>

    <style name="QuestionsImageVoted" >
        <item name="android:scaleType">centerInside</item>
        <item name="android:background">@color/highlight_black</item>
        <item name="android:src">@drawable/voted</item>
    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton"/>


</resources>
